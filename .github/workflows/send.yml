name: Telegram scheduled messages

on:
  schedule:
    # GitHub cron uses UTC. These are 09:00, 15:00, 19:00, 19:20, 19:30, 20:00 KST.
    - cron: "55 23 * * *"
    - cron: "55 5 * * *"
    - cron: "55 9 * * *"
    - cron: "15 10 * * *"
    - cron: "25 10 * * *"
    - cron: "55 10 * * *"
  workflow_dispatch:
    inputs:
      slot:
        description: Message slot
        required: true
        type: choice
        options: [09-00, 15-00, 19-00, 19-20, 19-30, 20-00]

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Select scheduled slot
        id: slot
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "value=${{ inputs.slot }}" >> "$GITHUB_OUTPUT"
          else
            case "${{ github.event.schedule }}" in
              "55 23 * * *") value="09-00" ;;
              "55 5 * * *") value="15-00" ;;
              "55 9 * * *") value="19-00" ;;
              "15 10 * * *") value="19-20" ;;
              "25 10 * * *") value="19-30" ;;
              "55 10 * * *") value="20-00" ;;
              *) exit 1 ;;
            esac
            echo "value=$value" >> "$GITHUB_OUTPUT"
          fi
      - name: Send Telegram message
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          WAIT_FOR_SLOT: ${{ github.event_name == 'schedule' && 'true' || 'false' }}
        run: node send.mjs "${{ steps.slot.outputs.value }}"
